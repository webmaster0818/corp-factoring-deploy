{
  "name": "seo-analytics",
  "version": "1.0.0",
  "description": "SEO Analytics Data Fetcher for GA4 and Search Console",
  "main": "index.js",
  "scripts": {
    "fetch-sc": "node scripts/fetch-search-console.js",
    "fetch-ga": "node scripts/fetch-ga4.js",
    "report": "node scripts/generate-report.js",
    "all": "npm run fetch-sc && npm run fetch-ga && npm run report"
  },
  "dependencies": {
    "@google-analytics/data": "^4.0.0",
    "googleapis": "^128.0.0",
    "dotenv": "^16.0.0"
  }
}
